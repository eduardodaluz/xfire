<project
  default="jar:install"
  xmlns:j="jelly:core"
  xmlns:ant="jelly:ant"
  xmlns:maven="jelly:maven"  
  xmlns:i="jelly:interaction">

  <ant:path id="xmlbeans.path">
    <ant:path refid="maven.dependency.classpath"/>
    <ant:pathelement path="${maven.build.dest}"/>
    <ant:pathelement path="${maven.test.dest}"/>
  </ant:path>
  
  
  <!-- 
    Generate XMLBeans objects for all the schemas in src/test-schemas 
    and make the WeatherService
   --> 
  <goal name="generate-main">
    
  	<!-- Generate XMLBeans -->
    <ant:delete dir="${maven.build.dir}/xfire-xmlbeans-main"/>
    <ant:mkdir dir="${maven.build.dir}/xfire-xmlbeans-main"/>
  
    <java classname="org.apache.xmlbeans.impl.tool.SchemaCompiler"
          classpathref="xmlbeans.path"
          fork="true">
      <arg value="-d"/>
      <arg value="${maven.build.dir}/xfire-xmlbeans-main"/>
      <arg value="${basedir}/src/wsdl/geoip.wsdl"/>
    </java>

  	<ant:delete dir="${maven.build.dir}/client"/>
  	<ant:mkdir dir="${maven.build.dir}/client"/>
  	
  	<!-- Setup WSGen classpath and taskdef -->
  	<ant:path id="wsgen.path">
  	  <ant:path refid="maven.dependency.classpath"/>
  	  <ant:pathelement path="${maven.build.dest}"/>
  	  <ant:pathelement path="${maven.test.dest}"/>
   	  <ant:pathelement path="${maven.build.dir}/xfire-xmlbeans-main"/>
  	</ant:path>
  	
  	<ant:taskdef name="wsgen" classname="org.codehaus.xfire.gen.WsGenTask" classpathref="wsgen.path"/>
  		
  	<!-- Run the client generator -->
  	<wsgen outputDirectory="${maven.build.dir}/client"
  	       wsdl="${basedir}/src/wsdl/geoip.wsdl"
  	       package="net.webservicex.weather"/>
  	
  	<copy todir="${maven.build.dest}">
      <fileset dir="${maven.build.dir}/xfire-xmlbeans-main/"/>
    </copy>

  	<ant:path id="services" location="${maven.build.dir}/client"/>
  	<maven:addPath id="maven.compile.src.set" refid="services"/>
  </goal>

  <preGoal name="java:compile">
    <attainGoal name="generate-main"/>
  </preGoal>
  
</project>